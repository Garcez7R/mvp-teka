export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// WhatsApp contact
export const WHATSAPP_DEFAULT = "+5551996263385";

// Generate login URL at runtime so redirect URI reflects the current origin.
export const getLoginUrl = () => "/login";
